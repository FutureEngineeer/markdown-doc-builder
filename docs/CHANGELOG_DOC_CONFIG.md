# Changelog - doc-config.yaml

## Версия 3.1.0 (2 декабря 2024)

### 🎉 Новый синтаксис

Введен новый, более компактный и читаемый синтаксис для `doc-config.yaml`.

#### Основные изменения

**До (старый формат):**
```yaml
hierarchy:
  - file: home.md
    title: "Home"
    description: "Главная страница"
```

**После (новый формат):**
```yaml
hierarchy:
  - Home:
      path: home.md
      description: "Главная страница"
```

**Краткий формат:**
```yaml
hierarchy:
  - Home: home.md
```

### ✨ Преимущества

1. **Компактность** - меньше строк кода
2. **Читаемость** - структура более понятна
3. **Гибкость** - легко переключаться между кратким и полным форматом
4. **Консистентность** - единый подход для всех типов элементов

### 📝 Новые возможности

#### 1. Краткий формат для простых элементов

```yaml
hierarchy:
  - Home: home.md
  - Docs: docs/
  - Repo: "https://github.com/user/repo"
```

#### 2. Полный формат с параметрами

```yaml
hierarchy:
  - Home:
      path: home.md
      description: "Главная страница"
      alias: home
```

#### 3. Секции с подразделами

```yaml
hierarchy:
  - Projects:
      section: true
      sub:
        - Alpha: project-alpha/
        - Beta: project-beta/
```

#### 4. Репозитории как секции

```yaml
hierarchy:
  - CLN Driver:
      path: "https://github.com/user/repo"
      section: true
      description: "Описание"
```

### 🔄 Обратная совместимость

Старый формат продолжает работать, но рекомендуется мигрировать на новый синтаксис.

### 📚 Документация

Добавлены новые файлы документации:

1. **[DOC_CONFIG_SYNTAX.md](DOC_CONFIG_SYNTAX.md)** - полное описание синтаксиса
2. **[DOC_CONFIG_CHEATSHEET.md](DOC_CONFIG_CHEATSHEET.md)** - шпаргалка
3. **[DOC_CONFIG_MIGRATION.md](DOC_CONFIG_MIGRATION.md)** - руководство по миграции

### 🔧 Технические изменения

#### Обновленные файлы

1. **components/docConfigProcessor.js**
   - Обновлен метод `parseHierarchyItem()` для поддержки нового синтаксиса
   - Сохранена обратная совместимость со старым форматом

2. **website/doc-config.yaml**
   - Мигрирован на новый синтаксис
   - Все элементы используют новый формат

3. **website/project-beta/doc-config.yaml**
   - Мигрирован на новый синтаксис

4. **test-site/doc-config.yaml**
   - Мигрирован на новый синтаксис

### 📊 Примеры миграции

#### Файл

**Было:**
```yaml
- file: home.md
  title: "Home"
```

**Стало:**
```yaml
- Home: home.md
```

#### Папка

**Было:**
```yaml
- folder: docs/
  title: "Documentation"
```

**Стало:**
```yaml
- Documentation: docs/
```

#### Репозиторий

**Было:**
```yaml
- repository: "https://github.com/user/repo"
  title: "My Repo"
  section: true
```

**Стало:**
```yaml
- My Repo:
    path: "https://github.com/user/repo"
    section: true
```

#### Секция

**Было:**
```yaml
- section: "Projects"
  children:
    - folder: alpha/
      title: "Alpha"
```

**Стало:**
```yaml
- Projects:
    section: true
    sub:
      - Alpha: alpha/
```

### 🎯 Рекомендации

1. Используйте **краткий формат** для простых элементов без дополнительных параметров
2. Используйте **полный формат** когда нужны `description`, `alias` или другие параметры
3. Мигрируйте существующие конфигурации постепенно
4. Проверяйте сборку после миграции: `node build-all-v3.js website`

### ✅ Проверка

После обновления запустите сборку:

```bash
node build-all-v3.js website
```

Успешная миграция покажет:
```
✓ Processed X doc-config files
✓ Found X files in hierarchy
✓ Found X repositories
```

### 🐛 Известные проблемы

Нет известных проблем.

### 📅 Следующие шаги

- Добавление валидации синтаксиса
- Автоматический конвертер старого формата в новый
- Расширенные примеры в документации

---

## Версия 3.0.0 (1 декабря 2024)

### Первоначальный релиз

- Базовая поддержка `doc-config.yaml`
- Старый формат с явными полями `file:`, `repository:`, `folder:`
- Поддержка секций с `children:`

---

**Последнее обновление:** 2 декабря 2024
