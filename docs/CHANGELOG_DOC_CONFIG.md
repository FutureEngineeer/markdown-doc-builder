# Changelog: Doc-Config Processing System

## Изменения

### ✅ Создан новый модуль `docConfigProcessor.js`

**Функциональность:**
- Рекурсивная обработка `doc-config.yaml` файлов
- Построение целостного дерева структуры
- Автоматическое сканирование папок без `doc-config.yaml`
- Поддержка вложенных конфигураций

**Ключевые методы:**
- `loadDocConfig()` - загрузка конфигурации
- `buildTreeForDirectory()` - рекурсивное построение дерева
- `buildTreeFromHierarchy()` - построение из hierarchy
- `buildTreeFromFileSystem()` - автоматическое сканирование
- `process()` - главный метод обработки

### ✅ Обновлен `build-all-v3.js`

**Phase 1: Indexing**
- Заменена старая логика `indexDirectory()` на `DocConfigProcessor`
- Теперь используется единое дерево структуры
- Репозитории извлекаются из дерева рекурсивно

**Phase 2: Navigation**
- Секции извлекаются из дерева автоматически
- Добавлено поле `tree` в `hierarchy-info.json`
- Сохраняется полная структура для навигации

### ✅ Удален fallback на `config.yaml`

**В `newSectionMap.js`:**
- Удалена функция `detectScopeItemType()`
- Удален блок обработки `config?.build?.scope`
- Теперь работает **только** с `hierarchy-info.json`

### ✅ Добавлено копирование `hierarchy-info.json` в `dist/`

**В `buildOrchestrator.js`:**
- Новый метод `copyHierarchyInfo()`
- Автоматически вызывается в `finishBuild()`
- Файл доступен в корне сайта для клиентских скриптов

## Новая логика

### До

```
1. Сканировать все .md файлы
2. Загрузить root doc-config.yaml
3. Загрузить doc-config.yaml из каждой папки
4. Скачать репозитории из всех конфигов
5. Построить структуру
```

### После

```
1. Запустить DocConfigProcessor
   ├─ Рекурсивно обработать doc-config.yaml
   ├─ Построить дерево структуры
   └─ Собрать все репозитории
2. Скачать репозитории из дерева
3. Сохранить дерево в hierarchy-info.json
```

## Преимущества

### 🎯 Целостность
- Одно дерево описывает всю структуру
- Нет дублирования логики
- Легко отследить иерархию

### 🔄 Рекурсивность
- Вложенные `doc-config.yaml` обрабатываются автоматически
- Каждая папка может переопределить свою структуру
- Поддержка любой глубины вложенности

### 🚀 Гибкость
- Контроль через `doc-config.yaml` где нужно
- Автоматическое сканирование где не нужно
- Смешанный подход в одном проекте

### 📦 Простота
- Меньше кода
- Понятная логика
- Легко расширять

## Обратная совместимость

✅ Все существующие `doc-config.yaml` работают без изменений
✅ Формат `hierarchy-info.json` расширен, но совместим
✅ Навигация работает как раньше
✅ Миграция не требуется

## Использование

### Базовый пример

```javascript
const { DocConfigProcessor } = require('./components/docConfigProcessor');

const processor = new DocConfigProcessor('./website');
const result = processor.process();

console.log('Tree:', result.tree);
console.log('Repositories:', result.repositories);
console.log('Files:', result.files);
```

### В build-all-v3.js

```javascript
// Phase 1
const docProcessor = new DocConfigProcessor(rootPath);
const docResult = docProcessor.process();

// Используем результат
const index = {
  files: docResult.files,
  allFiles: docResult.allFiles,
  docTree: docResult.tree,
  // ...
};
```

## Тестирование

Для тестирования новой системы:

1. Запустите сборку: `npm run build:all`
2. Проверьте `.temp/hierarchy-info.json` - должно быть поле `tree`
3. Проверьте `dist/hierarchy-info.json` - файл должен быть скопирован
4. Проверьте навигацию на сайте

## Документация

- `docs/DOC_CONFIG_PROCESSING.md` - полная документация системы
- `components/docConfigProcessor.js` - исходный код с комментариями
