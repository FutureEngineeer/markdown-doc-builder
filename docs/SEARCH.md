# Поисковый движок

## Обзор

Сайт оснащен полнофункциональным поисковым движком на базе [Lunr.js](https://lunrjs.com/), стилизованным в духе MkDocs Material.

## Возможности

- 🔍 **Полнотекстовый поиск** по всем страницам документации
- ⚡ **Мгновенные результаты** без перезагрузки страницы
- 🎯 **Умное ранжирование** с приоритетом заголовков
- 💡 **Подсветка совпадений** в результатах поиска
- ⌨️ **Горячие клавиши** для быстрого доступа
- 📱 **Адаптивный дизайн** для мобильных устройств

## Использование

### Открытие поиска

Есть несколько способов открыть поиск:

1. **Кнопка в header** - нажмите на кнопку "Поиск" в правом верхнем углу
2. **Горячая клавиша** - нажмите `Ctrl+K` (или `Cmd+K` на Mac)

### Навигация

- **Ввод текста** - начните вводить запрос (минимум 2 символа)
- **↑/↓** - навигация по результатам (планируется)
- **Enter** - переход на выбранную страницу (планируется)
- **Esc** - закрытие модального окна

### Советы по поиску

- Поиск работает по частичному совпадению (например, "докум" найдет "документация")
- Заголовки страниц имеют больший приоритет в результатах
- Breadcrumbs помогают понять контекст страницы
- Превью показывает фрагмент текста с подсветкой найденных слов

## Технические детали

### Архитектура

Поисковая система состоит из трех основных компонентов:

1. **Индексация** (`components/searchIndex.js`)
   - Сканирует все HTML файлы в `dist/`
   - Извлекает текстовый контент и метаданные
   - Создает поисковый индекс с помощью Lunr.js
   - Сохраняет индекс в `dist/search-index.json`

2. **Клиентский поиск** (`assets/scripts/search.js`)
   - Загружает индекс при первом открытии
   - Выполняет поиск в браузере без серверных запросов
   - Отображает результаты с подсветкой

3. **UI компоненты**
   - Кнопка поиска в header (`components/header.js`)
   - Модальное окно (`components/searchModal.js`)
   - Стили в стиле Material Design (`assets/styles/search.css`)

### Процесс сборки

Индекс генерируется автоматически при каждой сборке:

```bash
npm run build
```

Этапы:
1. Генерация HTML файлов
2. Сканирование всех HTML в `dist/`
3. Создание индекса с метаданными:
   - `title` - заголовок страницы
   - `content` - текстовое содержимое
   - `url` - относительный путь
   - `breadcrumb` - навигационная цепочка
   - `section` - раздел сайта

### Конфигурация индекса

Индекс настроен с приоритетами полей:

```javascript
this.field('title', { boost: 10 });      // Заголовки - наивысший приоритет
this.field('breadcrumb', { boost: 5 });  // Breadcrumbs
this.field('section', { boost: 3 });     // Разделы
this.field('content');                   // Основной контент
```

### Размер индекса

Индекс оптимизирован для быстрой загрузки:
- Сжатый JSON формат
- Только необходимые метаданные
- Типичный размер: ~50-200 KB для средних сайтов

## Кастомизация

### Стили

Стили поиска находятся в `assets/styles/search.css` и используют CSS переменные:

```css
--accent          /* Цвет подсветки */
--card-bg         /* Фон модального окна */
--text-primary    /* Основной текст */
--text-muted      /* Второстепенный текст */
```

### Настройка поиска

Для изменения поведения поиска отредактируйте `assets/scripts/search.js`:

- `maxLength` - длина превью (по умолчанию 150)
- `.slice(0, 10)` - количество результатов (по умолчанию 10)
- Wildcard `query + '*'` - для частичного совпадения

### Отключение поиска

Если нужно отключить поиск:

1. Удалите кнопку из `components/header.js`
2. Удалите импорт стилей из `assets/styles.css`
3. Удалите скрипт из `components/scriptLoader.js`

## Производительность

- **Индексация**: ~1-2 секунды для 100 страниц
- **Загрузка индекса**: ~100-500ms (зависит от размера)
- **Поиск**: <10ms для большинства запросов
- **Память**: ~5-10 MB для индекса в памяти

## Совместимость

- ✅ Chrome/Edge 90+
- ✅ Firefox 88+
- ✅ Safari 14+
- ✅ Мобильные браузеры

## Будущие улучшения

- [ ] Навигация по результатам с клавиатуры
- [ ] История поисковых запросов
- [ ] Поддержка фильтров по разделам
- [ ] Поиск по тегам
- [ ] Поддержка русского языка (стемминг)
- [ ] Fuzzy search для опечаток

## Troubleshooting

### Поиск не работает

1. Проверьте консоль браузера на ошибки
2. Убедитесь, что `search-index.json` существует в `dist/`
3. Проверьте, что lunr.js загружен (должен быть в `<head>`)

### Результаты не отображаются

1. Проверьте, что индекс содержит документы
2. Убедитесь, что запрос содержит минимум 2 символа
3. Проверьте стили - модальное окно может быть скрыто

### Индекс не генерируется

1. Убедитесь, что в `dist/` есть HTML файлы
2. Проверьте права на запись в `dist/`
3. Запустите сборку с флагом `--verbose` для отладки
