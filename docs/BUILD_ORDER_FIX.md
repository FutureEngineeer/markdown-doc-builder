# Исправление порядка сборки для корректного отображения репозиториев

## Проблема

На GitHub Pages репозитории (CLN, RadiX) не отображались в Site Map (hamburger menu) и Section Map, хотя:
- Файлы репозиториев генерировались корректно
- Локально всё работало правильно
- В консоли сборки структура отображалась корректно

## Причина

Проблема была в **порядке обработки файлов**:

1. Файлы обрабатывались последовательно в одном проходе
2. Когда генерировался первый локальный файл (например, `index.html`), вызывался `generateHamburgerMenu()`
3. `generateHamburgerMenu()` пытался прочитать директории репозиториев из `dist/CLN/` и `dist/project-beta/RadiX/`
4. Но эти директории ещё **не существовали**, так как файлы репозиториев обрабатывались позже
5. Результат: hamburger menu генерировался без репозиториев

### Почему локально работало?

Локально при повторной сборке директории репозиториев уже существовали в `dist/` от предыдущей сборки, поэтому `generateHamburgerMenu()` находил их и включал в меню.

На GitHub Pages каждая сборка происходит с нуля (после `npm run clean`), поэтому проблема проявлялась.

## Решение

Изменен порядок обработки файлов в `build-all-v3.js` на **двухпроходную систему**:

### Проход 1: Обработка репозиториев
```javascript
console.log('   📦 Pass 1: Processing repository files...');

async function processRepositories(item) {
  if (item.type === 'repository') {
    // Обрабатываем ВСЕ файлы репозитория
    // Создаём структуру директорий в dist/
  }
}

for (const item of fileStructure.root) {
  await processRepositories(item);
}
```

### Проход 2: Обработка локальных файлов
```javascript
console.log('   📄 Pass 2: Processing local files...');

async function processLocalFiles(item) {
  if (item.type === 'file') {
    // Обрабатываем локальные файлы
    // Теперь директории репозиториев уже существуют!
  }
}

for (const item of fileStructure.root) {
  await processLocalFiles(item);
}
```

## Результат

✅ **Проход 1** создаёт все директории и файлы репозиториев:
- `dist/CLN/` со всеми файлами
- `dist/project-beta/RadiX/` со всеми файлами

✅ **Проход 2** обрабатывает локальные файлы:
- При генерации `index.html` вызывается `generateHamburgerMenu()`
- Директории репозиториев уже существуют
- `generateHamburgerMenu()` находит их и включает в меню

## Проверка

### Локально:
```bash
npm run build:fresh
```

Вывод должен показывать:
```
📝 Phase 3: Generating files...
   📦 Pass 1: Processing repository files...
      Processing repository: CLN
      Processing repository: project-beta/RadiX
   📄 Pass 2: Processing local files...
```

### В hamburger menu:
- ✅ CLN Driver со всей структурой
- ✅ RadiX Project внутри project-beta
- ✅ Все файлы и папки репозиториев

### На GitHub Pages:
После деплоя проверить:
- Site Map (hamburger menu) содержит репозитории
- Section Map на страницах репозиториев работает корректно
- Все ссылки работают

## Дополнительные исправления

В рамках этого PR также исправлены:
1. **Двойной слеш в URL** - `./index.html` вместо `.//index.html`
2. **Нормализация имён файлов** - все HTML файлы в нижнем регистре
3. **Обработка репозиториев в hamburgerMenu.js** - правильное добавление в дерево

## Дата исправления
1 декабря 2025
