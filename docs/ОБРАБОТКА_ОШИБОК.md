# Обработка ошибок на сервере

## 📋 Обзор

Проект настроен для автоматической обработки ошибок HTTP и отображения красивых страниц ошибок вместо стандартных сообщений сервера.

## 🎯 Реализованные страницы ошибок

### 1. **404 - Страница не найдена**
- **Файл:** `assets/errors/404.html`
- **Когда показывается:** Пользователь переходит по несуществующему URL
- **Особенности:**
  - Анимированный фон с градиентами
  - Кнопки "На главную" и "Назад"
  - Полезные ссылки на основные разделы
  - Адаптивный дизайн для мобильных устройств

### 2. **500 - Внутренняя ошибка сервера**
- **Файл:** `assets/errors/500.html`
- **Когда показывается:** Внутренние ошибки сервера
- **Особенности:**
  - Анимированный фон с красными оттенками
  - Кнопки "На главную" и "Обновить"
  - Информация о возможных действиях
  - Советы по решению проблемы

## ⚙️ Конфигурация Netlify

Обработка ошибок настроена в файле `netlify.toml`:

```toml
# SPA fallback - для правильной работы роутинга
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = false
  
# Custom error pages
[[redirects]]
  from = "/*"
  to = "/assets/errors/404.html"
  status = 404

# Server error page  
[[redirects]]
  from = "/*"
  to = "/assets/errors/500.html"
  status = 500
```

### Как это работает:

1. **SPA Fallback (200):** Все запросы сначала пытаются найти реальный файл. Если файл не найден, запрос перенаправляется на `index.html` для обработки клиентским роутингом.

2. **404 Error:** Если страница действительно не существует (даже после SPA fallback), показывается кастомная страница 404.

3. **500 Error:** При серверных ошибках автоматически показывается страница 500.

## 🔧 Процесс сборки

Страницы ошибок автоматически копируются в папку `dist` при сборке проекта:

```javascript
// В build-all-v2.js
function copyRecursive(source, target) {
  // Копирует всю папку assets, включая assets/errors
}

const sourceDir = 'assets';
const targetDir = 'dist/assets';
copyRecursive(sourceDir, targetDir);
```

## 🧪 Тестирование

### Локальное тестирование:

1. Откройте файл `test-error-pages.html` в браузере
2. Проверьте обе страницы ошибок
3. Убедитесь, что все ссылки работают

### Тестирование на Netlify:

1. Перейдите по несуществующему URL: `https://ваш-сайт.netlify.app/несуществующая-страница`
2. Должна показаться страница 404

## 🎨 Дизайн страниц ошибок

### Общие особенности:
- **Темная тема** в стиле основного сайта (#1B1C28)
- **Анимированный фон** с градиентами
- **Адаптивный дизайн** для всех устройств
- **Брендовые цвета:** зеленый (#2FB65A) и синий (#5865F2)
- **Плавные анимации** для улучшения UX

### Анимации:
- Плавное появление контента (fadeIn)
- Пульсация кода ошибки
- Анимация иконок (bounce, shake)
- Hover-эффекты на кнопках

## 📝 Добавление новых страниц ошибок

Если нужно добавить другие коды ошибок (например, 403):

1. Создайте файл `assets/errors/403.html`
2. Добавьте редирект в `netlify.toml`:
   ```toml
   [[redirects]]
     from = "/*"
     to = "/assets/errors/403.html"
     status = 403
   ```
3. Пересоберите проект: `npm run build`

## 🚀 Деплой

При деплое на Netlify:
1. Все файлы из `assets/errors/` копируются в `dist/assets/errors/`
2. Netlify автоматически применяет правила из `netlify.toml`
3. Страницы ошибок начинают работать сразу после деплоя

## 🔍 Отладка

Если страницы ошибок не работают:

1. **Проверьте сборку:**
   ```bash
   npm run build
   ```
   Убедитесь, что файлы скопированы в `dist/assets/errors/`

2. **Проверьте netlify.toml:**
   - Правильный порядок редиректов (SPA fallback должен быть первым)
   - Правильные пути к файлам ошибок

3. **Проверьте логи Netlify:**
   - Откройте панель Netlify
   - Перейдите в раздел "Deploy logs"
   - Проверьте наличие ошибок при деплое

## 📚 Дополнительные ресурсы

- [Netlify Redirects Documentation](https://docs.netlify.com/routing/redirects/)
- [Custom Error Pages](https://docs.netlify.com/routing/redirects/redirect-options/#custom-404-page)
- [SPA Fallback](https://docs.netlify.com/routing/redirects/rewrites-proxies/#history-pushstate-and-single-page-apps)
