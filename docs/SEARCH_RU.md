# Поиск по сайту

## Быстрый старт

Поиск автоматически включен на всех страницах сайта.

### Как открыть поиск

- **Кнопка** - нажмите на иконку 🔍 в правом верхнем углу
- **Горячая клавиша** - `Ctrl+K` (Windows/Linux) или `Cmd+K` (Mac)

### Как искать

1. Откройте поиск
2. Начните вводить запрос (минимум 2 символа)
3. Результаты появятся мгновенно
4. Кликните на результат для перехода на страницу
5. Нажмите `Esc` для закрытия

## Особенности

- ⚡ **Мгновенный поиск** - результаты без задержки
- 🎯 **Умное ранжирование** - заголовки в приоритете
- 💡 **Подсветка** - найденные слова выделены
- 📱 **Мобильная версия** - работает на всех устройствах

## Технические детали

Поиск работает на базе [Lunr.js](https://lunrjs.com/) - легковесной библиотеки для полнотекстового поиска в браузере.

### Что индексируется

- Заголовки страниц (наивысший приоритет)
- Навигационные цепочки (breadcrumbs)
- Названия разделов
- Весь текстовый контент страниц

### Производительность

- Индекс загружается только при первом открытии поиска
- Поиск выполняется локально в браузере
- Нет серверных запросов - работает даже офлайн

## Для разработчиков

Подробная документация по настройке и кастомизации: [SEARCH.md](./SEARCH.md)

### Файлы

- `components/searchIndex.js` - генерация индекса
- `assets/scripts/search.js` - клиентский поиск
- `assets/styles/search.css` - стили
- `components/searchModal.js` - UI модального окна
- `dist/search-index.json` - поисковый индекс (генерируется автоматически)

### Сборка

Индекс создается автоматически при каждой сборке:

```bash
npm run build
```

Для проверки индекса:

```bash
# Количество проиндексированных документов
node -e "console.log(require('./dist/search-index.json').documents.length)"
```
