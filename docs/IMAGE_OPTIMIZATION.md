# Оптимизация изображений

## Автоматическое сжатие

Система сборки теперь автоматически оптимизирует все изображения в папке `dist/assets` после генерации HTML файлов.

### Поддерживаемые форматы

- **JPEG/JPG** - сжатие с качеством 85% (mozjpeg)
- **PNG** - сжатие с качеством 70-90% (pngquant)
- **GIF** - оптимизация уровня 2 (gifsicle)
- **SVG** - оптимизация с сохранением viewBox и ID (svgo)

### Установка зависимостей

Пакеты для оптимизации изображений уже включены в `package.json`:

```bash
npm install
```

Если пакеты не установлены, система покажет предупреждение и пропустит оптимизацию.

### Результаты

После сборки вы увидите статистику оптимизации:

```
🖼️  Optimizing images...
   ✓ Processed 20 images
   ℹ️  Found 10 large images (>100KB)
   ✓ Saved 5806.7KB (35.7%)
```

### Настройка качества

Чтобы изменить параметры сжатия, отредактируйте функцию `optimizeImages()` в файле `build-all-v2.js`:

```javascript
// JPEG качество (0-100)
imageminMozjpeg({ quality: 85 })

// PNG качество (0-1)
imageminPngquant({ quality: [0.7, 0.9] })

// GIF уровень оптимизации (1-3)
imageminGifsicle({ optimizationLevel: 2 })
```

## Скрытие списка файлов

Список сгенерированных HTML файлов теперь скрыт для более чистого вывода. Показываются только:

- Структура файлов (Phase 2)
- Ошибки генерации
- Итоговая статистика

Это ускоряет процесс сборки и делает вывод более читаемым.
