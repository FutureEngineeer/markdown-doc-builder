# Возможности поиска

## Интерфейс

### Кнопка поиска в header

Кнопка поиска всегда видна в правом верхнем углу header:

```
┌─────────────────────────────────────────────────────────┐
│ 🏠 Logo  Sitename          Nav Links    [🔍 Поиск Ctrl K]│
└─────────────────────────────────────────────────────────┘
```

**Особенности:**
- Всегда доступна на всех страницах
- Показывает горячую клавишу `Ctrl K`
- На мобильных устройствах отображается только иконка
- Адаптивный дизайн

### Модальное окно поиска

При нажатии на кнопку или `Ctrl+K` открывается модальное окно:

```
┌─────────────────────────────────────────────────────────┐
│  🔍  [Поиск в документации...]                      [×]  │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  📄 CLN17 V2.0 HARDWARE              CLN / CLN17 V2.0   │
│     ...текст с подсветкой найденных слов...             │
│                                                          │
│  📄 Enclosure Design                 CLN / ENCLOSURE    │
│     ...превью с контекстом...                           │
│                                                          │
│  📄 API Reference                    Проекты / Beta     │
│     ...релевантный фрагмент...                          │
│                                                          │
├─────────────────────────────────────────────────────────┤
│  ↑↓ навигация  Enter выбрать  Esc закрыть  Lunr.js     │
└─────────────────────────────────────────────────────────┘
```

## Функциональность

### 1. Мгновенный поиск

- Результаты появляются по мере ввода
- Минимум 2 символа для начала поиска
- Нет задержек и перезагрузок страницы

### 2. Умное ранжирование

Приоритеты при поиске:
1. **Заголовки страниц** (boost: 10) - наивысший приоритет
2. **Breadcrumbs** (boost: 5) - навигационные цепочки
3. **Разделы** (boost: 3) - названия разделов
4. **Контент** (boost: 1) - основной текст

### 3. Подсветка совпадений

Найденные слова выделяются в:
- Заголовках результатов
- Превью текста
- Используется контрастный цвет для лучшей видимости

### 4. Контекстные превью

Каждый результат показывает:
- **Заголовок** - название страницы
- **Breadcrumb** - путь к странице (например, "CLN / Hardware")
- **Превью** - фрагмент текста с найденными словами (150 символов)

### 5. Горячие клавиши

| Клавиша | Действие |
|---------|----------|
| `Ctrl+K` / `Cmd+K` | Открыть поиск |
| `Esc` | Закрыть поиск |
| `↑` / `↓` | Навигация (планируется) |
| `Enter` | Переход (планируется) |

## Технические особенности

### Производительность

- **Индексация**: ~1-2 сек для 100 страниц
- **Загрузка индекса**: ~100-500ms (зависит от размера)
- **Поиск**: <10ms для большинства запросов
- **Размер индекса**: ~50-200 KB (сжатый JSON)

### Оффлайн работа

Поиск работает полностью в браузере:
- Нет серверных запросов
- Работает даже без интернета (после загрузки)
- Быстрый отклик

### Адаптивность

**Десктоп:**
- Полная кнопка с текстом и горячей клавишей
- Модальное окно по центру экрана
- Показываются все подсказки

**Планшет:**
- Кнопка без текста, только иконка
- Адаптированное модальное окно
- Упрощенные подсказки

**Мобильные:**
- Только иконка поиска
- Полноэкранное модальное окно
- Оптимизированный ввод

## Примеры использования

### Поиск по ключевым словам

**Запрос:** `stepper motor`

**Результаты:**
- Страницы с "stepper" и "motor" в заголовках
- Документы с этими словами в тексте
- Релевантные разделы

### Поиск по коду продукта

**Запрос:** `CLN17`

**Результаты:**
- Все страницы о CLN17
- Спецификации версий
- Документация по железу

### Поиск по разделам

**Запрос:** `API`

**Результаты:**
- API документация
- Справочники
- Примеры использования

## Кастомизация

### Изменение количества результатов

В `assets/scripts/search.js`:

```javascript
return results.map(result => {
  // ...
}).slice(0, 10); // Измените 10 на нужное число
```

### Изменение длины превью

В `assets/scripts/search.js`:

```javascript
function generatePreview(content, query) {
  const maxLength = 150; // Измените на нужную длину
  // ...
}
```

### Настройка приоритетов

В `components/searchIndex.js`:

```javascript
this.field('title', { boost: 10 });      // Заголовки
this.field('breadcrumb', { boost: 5 });  // Breadcrumbs
this.field('section', { boost: 3 });     // Разделы
this.field('content');                   // Контент
```

### Стилизация

Все стили в `assets/styles/search.css` используют CSS переменные:

```css
--accent          /* Цвет подсветки */
--card-bg         /* Фон модального окна */
--text-primary    /* Основной текст */
--text-muted      /* Второстепенный текст */
--border-color    /* Цвет границ */
```

## Ограничения

### Текущие

- Нет навигации по результатам с клавиатуры
- Нет истории поисковых запросов
- Нет фильтров по разделам
- Базовая поддержка русского языка (без стемминга)

### Планируемые улучшения

- [ ] Навигация с клавиатуры (↑↓ Enter)
- [ ] История последних запросов
- [ ] Фильтры по разделам/тегам
- [ ] Поддержка русского стемминга
- [ ] Fuzzy search для опечаток
- [ ] Поиск по тегам
- [ ] Экспорт результатов

## Troubleshooting

### Поиск не открывается

**Проблема:** Кнопка не реагирует или `Ctrl+K` не работает

**Решение:**
1. Проверьте консоль браузера на ошибки
2. Убедитесь, что `search.js` загружен
3. Проверьте, что `lunr.min.js` загружен

### Нет результатов

**Проблема:** Поиск не находит очевидные совпадения

**Решение:**
1. Проверьте, что `search-index.json` существует
2. Убедитесь, что индекс содержит документы
3. Попробуйте другие ключевые слова
4. Проверьте минимальную длину запроса (2 символа)

### Медленная загрузка

**Проблема:** Индекс загружается долго

**Решение:**
1. Проверьте размер `search-index.json`
2. Оптимизируйте контент (удалите лишние страницы)
3. Используйте CDN для статики
4. Включите gzip сжатие на сервере

### Неправильное ранжирование

**Проблема:** Нерелевантные результаты в топе

**Решение:**
1. Настройте приоритеты полей в `searchIndex.js`
2. Улучшите заголовки страниц
3. Добавьте больше контекста в breadcrumbs
4. Используйте теги для категоризации
