# Отладка поиска

## Проверка работы поиска

### 1. Откройте консоль браузера

**Chrome/Edge:**
- Нажмите `F12` или `Ctrl+Shift+I`
- Перейдите на вкладку "Console"

**Firefox:**
- Нажмите `F12` или `Ctrl+Shift+K`
- Перейдите на вкладку "Консоль"

### 2. Откройте поиск

Нажмите `Ctrl+K` или кликните на кнопку поиска в header.

### 3. Проверьте логи

В консоли должны появиться сообщения:

```
🔍 Открытие модального окна поиска
⏳ Инициализация поиска...
🔍 Загрузка индекса: ./search-index.json (или ../search-index.json)
✅ Поисковый индекс загружен: 42 документов
```

### 4. Введите запрос

Введите любой запрос (минимум 2 символа), например "CLN".

В консоли должно появиться:

```
🔎 Поиск: CLN
✅ Найдено результатов: 15
```

## Частые проблемы

### Проблема 1: Модальное окно не открывается

**Симптомы:**
- Кнопка поиска не реагирует
- `Ctrl+K` не работает

**Проверка:**
```javascript
// В консоли браузера
document.getElementById('search-modal')
// Должно вернуть элемент, а не null
```

**Решение:**
1. Проверьте, что `search-modal` есть в HTML
2. Убедитесь, что `searchModal.js` подключен в `converter.js`
3. Пересоберите проект: `npm run build`

### Проблема 2: Ошибка загрузки индекса

**Симптомы:**
```
❌ Ошибка загрузки поискового индекса: HTTP 404: Not Found
```

**Проверка:**
```bash
# Проверьте наличие индекса
ls dist/search-index.json

# Проверьте размер
ls -lh dist/search-index.json
```

**Решение:**
1. Убедитесь, что индекс создан: `npm run search:test`
2. Пересоберите: `npm run build`
3. Проверьте путь в консоли браузера

### Проблема 3: Lunr.js не загружен

**Симптомы:**
```
❌ Ошибка: lunr is not defined
```

**Проверка:**
```javascript
// В консоли браузера
typeof lunr
// Должно вернуть "function"
```

**Решение:**
1. Проверьте, что Lunr.js подключен в HTML:
```html
<script src="https://cdn.jsdelivr.net/npm/lunr@2.3.9/lunr.min.js"></script>
```
2. Проверьте интернет-соединение (Lunr загружается с CDN)
3. Убедитесь, что `scriptLoader.js` добавляет Lunr в зависимости

### Проблема 4: Неправильные пути на вложенных страницах

**Симптомы:**
- Поиск работает на главной странице
- Не работает на вложенных страницах (например, `/CLN/wiki/features.html`)
- Ошибка 404 при загрузке индекса

**Проверка:**
Откройте вложенную страницу и проверьте в консоли:
```
🔍 Загрузка индекса: ../search-index.json
```

Путь должен быть правильным для текущей глубины вложенности.

**Решение:**
Функция `getSearchIndexPath()` в `search.js` должна правильно вычислять путь:

```javascript
function getSearchIndexPath() {
  const path = window.location.pathname;
  const depth = (path.match(/\//g) || []).length - 1;
  
  if (depth <= 1) {
    return './search-index.json';
  }
  
  return '../'.repeat(depth - 1) + 'search-index.json';
}
```

### Проблема 5: Результаты не отображаются

**Симптомы:**
- Поиск выполняется (видно в консоли)
- Результаты не появляются в модальном окне

**Проверка:**
```javascript
// В консоли браузера
document.getElementById('search-results')
// Должно вернуть элемент
```

**Решение:**
1. Проверьте стили `search.css`
2. Убедитесь, что `handleSearchInput` вызывается
3. Проверьте, что результаты генерируются правильно

### Проблема 6: Ссылки в результатах не работают

**Симптомы:**
- Результаты отображаются
- Клик по результату ведёт на 404

**Проверка:**
Проверьте URL в результатах (наведите мышь на ссылку).

**Решение:**
Функция `getResultUrl()` должна правильно преобразовывать URL:

```javascript
function getResultUrl(resultUrl) {
  const path = window.location.pathname;
  const depth = (path.match(/\//g) || []).length - 1;
  let url = resultUrl.replace(/^\.\//, '');
  
  if (depth <= 1) {
    return url;
  }
  
  return '../'.repeat(depth - 1) + url;
}
```

## Тестирование

### Автоматический тест

```bash
npm run search:test
```

Должен вывести:
```
✅ Индекс Lunr создан успешно
✅ Все файлы поиска на месте!
✅ Поиск полностью интегрирован!
```

### Ручной тест

1. Откройте `test-search.html` в браузере
2. Нажмите все кнопки по порядку
3. Все тесты должны пройти успешно (✅)

### Тест путей

```bash
node scripts/test-search-paths.js
```

Проверяет правильность вычисления путей для разных уровней вложенности.

## Отладочные команды

### Проверка индекса в консоли браузера

```javascript
// Загрузить индекс вручную
fetch('./search-index.json')
  .then(r => r.json())
  .then(data => {
    console.log('Документов:', data.documents.length);
    console.log('Первый документ:', data.documents[0]);
  });

// Проверить Lunr
console.log('Lunr версия:', lunr.version);

// Проверить инициализацию поиска
console.log('Поиск инициализирован:', searchInitialized);

// Выполнить тестовый поиск
if (searchInitialized) {
  const results = performSearch('test');
  console.log('Результаты:', results);
}
```

### Проверка элементов DOM

```javascript
// Проверить модальное окно
console.log('Modal:', document.getElementById('search-modal'));

// Проверить input
console.log('Input:', document.getElementById('search-input'));

// Проверить контейнер результатов
console.log('Results:', document.getElementById('search-results'));

// Проверить кнопку поиска
console.log('Button:', document.querySelector('.search-button'));
```

## Логирование

Для включения подробного логирования добавьте в начало `search.js`:

```javascript
const DEBUG = true;

function log(...args) {
  if (DEBUG) console.log(...args);
}
```

И замените `console.log` на `log` в нужных местах.

## Производительность

### Проверка времени загрузки индекса

```javascript
console.time('index-load');
await initSearch();
console.timeEnd('index-load');
// Должно быть < 500ms
```

### Проверка времени поиска

```javascript
console.time('search');
const results = performSearch('test');
console.timeEnd('search');
// Должно быть < 10ms
```

## Известные ограничения

1. **Размер индекса**: Для очень больших сайтов (>1000 страниц) индекс может быть большим (>5 MB)
2. **Русский язык**: Нет стемминга для русского языка (поиск по точным словам)
3. **Опечатки**: Нет fuzzy search (поиск с опечатками)
4. **Фильтры**: Нет фильтрации по разделам/тегам

## Получение помощи

Если проблема не решена:

1. Проверьте все пункты выше
2. Соберите информацию:
   - Версия браузера
   - Логи из консоли
   - Путь к проблемной странице
   - Скриншоты
3. Создайте issue в GitHub репозитории
4. Опишите проблему подробно с примерами

## Полезные ссылки

- [Lunr.js документация](https://lunrjs.com/)
- [Техническая документация поиска](./SEARCH.md)
- [Руководство пользователя](./USER_GUIDE_SEARCH.md)
