# Обработка изображений

## Что делает система

Система **автоматически** обрабатывает все изображения:

### ✅ Из локальных markdown файлов
- Находит все изображения в `![alt](path)` 
- Копирует в `dist/assets/images/`
- Оптимизирует (сжимает без потери качества)

### ✅ Из GitHub репозиториев
- Скачивает изображения вместе с markdown файлами
- Оптимизирует перед сохранением
- Сохраняет в `dist/assets/images/`

### ✅ Оптимизация
- **Качество**: 85% (оптимальный баланс)
- **Размер**: максимум 1920x1080 (сохраняет пропорции)
- **Метаданные**: удаляются (EXIF, IPTC)
- **PNG**: максимальное сжатие
- **SVG/ICO**: копируются без изменений

## Установка ImageMagick (рекомендуется)

Для полной оптимизации установите ImageMagick:

**Windows:**
```bash
# Скачайте с https://imagemagick.org/script/download.php#windows
```

**macOS:**
```bash
brew install imagemagick
```

**Linux:**
```bash
sudo apt-get install imagemagick
```

**Проверка:**
```bash
magick -version
```

> **Без ImageMagick**: изображения будут скопированы без оптимизации

## Поддерживаемые форматы

- PNG
- JPG/JPEG
- GIF
- SVG
- WebP
- BMP
- ICO

## Примеры

### В markdown файле

```markdown
![Логотип](./images/logo.png)
![Скриншот](../screenshots/demo.png)
```

### Из GitHub

```markdown
![Диаграмма](docs/architecture.png)
![Demo](https://github.com/owner/repo/blob/main/demo.gif)
```

Все ссылки автоматически конвертируются в правильные пути.

## Статистика

После сборки выводится:

```
📸 Processing images from markdown files...
   Files processed: 15
   Images found: 42
   Optimized: 38
   Copied: 4
```

## Структура

```
dist/
└── assets/
    └── images/
        ├── owner1-repo1/
        │   └── docs/
        │       └── screenshot.png
        ├── owner2-repo2/
        │   └── logo.svg
        └── website/
            └── project-name/
                └── image.png
```

**Структура:**
- GitHub: `{owner}-{repo}/{путь}`
- Локальные: `{секция}/{путь}`

## Индексация и дедупликация

- **Хеширование**: SHA-256 для каждого изображения
- **Дедупликация**: одинаковые файлы сохраняются один раз
- **Индекс**: `.temp/image-index.json`
- **Экономия**: автоматический подсчет сэкономленного места

## Кеширование

- Изображения из GitHub кешируются на 12 часов
- Кеш: `.temp/repos-cache.json`
- При повторной сборке не скачиваются заново

## Настройка

Параметры в коде:

```javascript
optimizeImage(source, target, {
  quality: 85,        // 0-100
  maxWidth: 1920,     // пиксели
  maxHeight: 1080,    // пиксели
  stripMetadata: true // удалять метаданные
});
```

## Устранение проблем

### Изображение не найдено
- Проверьте путь в markdown
- Проверьте существование файла

### Не оптимизировано
- Установите ImageMagick
- Проверьте формат (SVG/ICO не оптимизируются)

### Большой размер
- Установите ImageMagick
- Уменьшите `quality` или `maxWidth/maxHeight`

---

**Подробная документация**: `docs/IMAGE_PROCESSING.md`
