# GitHub Pages Deployment Guide

## Автоматический деплой

Проект настроен на автоматический деплой на GitHub Pages через GitHub Actions.

### Когда происходит деплой:

1. **По расписанию** - каждые 12 часов (автоматически обновляет контент из GitHub репозиториев)
2. **Вручную** - через интерфейс GitHub Actions (вкладка Actions → Build and Deploy → Run workflow)

### Процесс деплоя:

```
1. Checkout кода
2. Установка Node.js 18
3. Установка зависимостей (npm ci)
4. 🧹 Очистка временных файлов (.temp, temp, dist)
5. 🚀 Сборка сайта (npm run build:all)
6. 📦 Загрузка артефакта
7. 🌐 Деплой на GitHub Pages
```

### Важно:

- **Каждая сборка происходит с нуля** - все временные файлы и кеш очищаются перед сборкой
- Это гарантирует актуальность контента из GitHub репозиториев
- Сборка занимает ~1-2 минуты

## Ручной запуск деплоя

1. Перейдите на вкладку **Actions** в репозитории
2. Выберите workflow **Build and Deploy**
3. Нажмите **Run workflow**
4. Выберите ветку (обычно `main`)
5. Нажмите зеленую кнопку **Run workflow**

## Проверка статуса деплоя

1. Перейдите на вкладку **Actions**
2. Найдите последний запуск **Build and Deploy**
3. Проверьте статус:
   - ✅ Зеленая галочка - деплой успешен
   - ❌ Красный крестик - деплой провалился (смотрите логи)
   - 🟡 Желтый круг - деплой в процессе

## Настройка GitHub Pages

Если деплой не работает, проверьте настройки:

1. Перейдите в **Settings** → **Pages**
2. В разделе **Source** выберите:
   - Source: **GitHub Actions**
3. Сохраните изменения

## URL сайта

После успешного деплоя сайт доступен по адресу:
```
https://<username>.github.io/<repository-name>/
```

Для этого проекта:
```
https://futureengineeer.github.io/markdown-doc-builder/
```

## Troubleshooting

### Деплой провалился

1. Проверьте логи в Actions
2. Убедитесь, что все зависимости установлены
3. Проверьте, что `website/config.yaml` существует
4. Проверьте, что все репозитории доступны

### Сайт не обновляется

1. Подождите 1-2 минуты после деплоя
2. Очистите кеш браузера (Ctrl+F5)
3. Проверьте, что деплой завершился успешно

### Ошибка "Root path is required"

Проверьте, что в `package.json` команда `build:all` содержит:
```json
"build:all": "node build-all-v3.js website"
```

## Локальная сборка

Для тестирования перед деплоем:

```bash
# Сборка с нуля (очистка + сборка)
npm run build:fresh

# Или по отдельности:
npm run clean      # Очистка .temp, temp, dist
npm run build:all  # Сборка

# Проверка результата
ls -la dist/
```

## Дата создания
1 декабря 2025
