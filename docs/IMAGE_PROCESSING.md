# Обработка изображений

Система автоматически обрабатывает изображения из markdown файлов и репозиториев GitHub.

## Основные возможности

### 1. Автоматическое копирование изображений

Все изображения из markdown файлов автоматически копируются в `dist/assets/images/` с сохранением структуры папок.

**Поддерживаемые форматы:**
- PNG
- JPG/JPEG
- GIF
- SVG
- WebP
- BMP
- ICO

### 2. Оптимизация изображений

При копировании изображения автоматически оптимизируются:

- **Сжатие**: качество 85% (оптимальный баланс размер/качество)
- **Изменение размера**: максимум 1920x1080 (сохраняет пропорции)
- **Удаление метаданных**: EXIF, IPTC и другие метаданные удаляются
- **PNG оптимизация**: максимальный уровень сжатия

**Примечание:** SVG и ICO файлы копируются без изменений.

### 3. Обработка изображений из GitHub

Изображения из GitHub репозиториев:
- Автоматически скачиваются при загрузке репозитория
- Оптимизируются перед сохранением
- Сохраняются в `dist/assets/images/{owner-repo}/` с исходной структурой папок
- Дедуплицируются - одинаковые изображения сохраняются только один раз

### 4. Обработка ссылок на изображения

Система автоматически обрабатывает различные типы ссылок:

```markdown
# Относительные ссылки
![Alt text](./images/photo.png)
![Alt text](../images/photo.png)

# Абсолютные ссылки в репозитории
![Alt text](/docs/images/photo.png)

# GitHub ссылки (из того же репозитория)
![Alt text](https://github.com/owner/repo/blob/main/images/photo.png)
```

Все ссылки автоматически конвертируются в правильные относительные пути к `assets/images/`.

## Требования

### ImageMagick (опционально)

Для полной оптимизации изображений рекомендуется установить ImageMagick:

**Windows:**
```bash
# Скачайте установщик с официального сайта
https://imagemagick.org/script/download.php#windows
```

**macOS:**
```bash
brew install imagemagick
```

**Linux:**
```bash
sudo apt-get install imagemagick
```

**Проверка установки:**
```bash
magick -version
```

### Работа без ImageMagick

Если ImageMagick не установлен:
- Изображения будут скопированы без оптимизации
- Система выведет предупреждение для файлов больше 5MB
- Все функции будут работать корректно

## Настройка оптимизации

Параметры оптимизации можно изменить в коде:

```javascript
optimizeImage(sourcePath, targetPath, {
  quality: 85,        // Качество (0-100)
  maxWidth: 1920,     // Максимальная ширина
  maxHeight: 1080,    // Максимальная высота
  stripMetadata: true // Удалять метаданные
});
```

## Статистика обработки

После сборки выводится статистика:

```
📸 Processing images from markdown files...
   Files processed: 15
   Images found: 42
   Optimized: 38
   Copied: 4
```

- **Files processed**: количество обработанных markdown файлов
- **Images found**: общее количество найденных изображений
- **Optimized**: количество оптимизированных изображений
- **Copied**: количество скопированных без оптимизации

## Индексация и дедупликация

Система индексирует все изображения для предотвращения дублирования:

### Как это работает

1. **Хеширование**: каждое изображение получает уникальный SHA-256 хеш
2. **Проверка**: перед сохранением проверяется, существует ли уже файл с таким хешем
3. **Дедупликация**: если файл существует, используется существующий путь
4. **Индекс**: информация сохраняется в `.temp/image-index.json`

### Преимущества

- **Экономия места**: одинаковые изображения сохраняются только один раз
- **Быстрая проверка**: хеш вычисляется один раз
- **Статистика**: автоматически вычисляется сэкономленное пространство

### Индексный файл

`.temp/image-index.json` содержит:
- Список всех уникальных изображений с хешами
- Карту путей (исходный путь → финальный путь)
- Список файлов, использующих каждое изображение
- Статистику (всего, уникальных, дубликатов, сэкономлено места)

## Кеширование

Изображения из GitHub репозиториев кешируются:
- Кеш хранится в `.temp/repos-cache.json`
- Время жизни кеша: 12 часов
- При повторной сборке изображения не скачиваются заново

## Структура папок

```
dist/
└── assets/
    └── images/
        ├── owner1-repo1/
        │   ├── docs/
        │   │   └── screenshot.png
        │   └── logo.svg
        ├── owner2-repo2/
        │   └── banner.jpg
        └── website/
            └── project-name/
                └── docs/
                    └── image.png
```

Структура папок:
- **GitHub репозитории**: `{owner}-{repo}/{original-path}`
- **Локальные файлы**: `{section-path}/{original-path}`

Это предотвращает конфликты имен файлов из разных источников.

## Примеры использования

### В markdown файле

```markdown
# Мой проект

![Логотип](./images/logo.png)

## Скриншоты

![Главная страница](./screenshots/home.png)
![Настройки](./screenshots/settings.png)
```

### В GitHub репозитории

```markdown
# Project

![Architecture](docs/architecture.png)
![Demo](https://github.com/owner/repo/blob/main/demo.gif)
```

Все изображения будут автоматически обработаны и доступны в финальной сборке.

## Устранение проблем

### Изображение не найдено

Проверьте:
1. Путь к изображению в markdown файле
2. Существует ли файл в исходной директории
3. Правильность относительного пути

### Изображение не оптимизировано

Возможные причины:
1. ImageMagick не установлен (изображение будет скопировано)
2. Формат не поддерживается для оптимизации (SVG, ICO)
3. Ошибка при оптимизации (изображение будет скопировано)

### Большой размер изображений

Если изображения занимают много места:
1. Установите ImageMagick для автоматической оптимизации
2. Уменьшите параметр `quality` в настройках
3. Уменьшите `maxWidth` и `maxHeight`

## API

### optimizeImage(inputPath, outputPath, options)

Оптимизирует одно изображение.

**Параметры:**
- `inputPath` (string): путь к исходному изображению
- `outputPath` (string): путь для сохранения
- `options` (object): опции оптимизации

**Возвращает:**
```javascript
{
  optimized: true,
  originalSize: 1024000,
  optimizedSize: 512000,
  savedBytes: 512000,
  savedPercent: "50.0"
}
```

### processMarkdownImages(markdownPath, markdown, distDir)

Обрабатывает изображения из markdown файла.

**Параметры:**
- `markdownPath` (string): путь к markdown файлу
- `markdown` (string): содержимое markdown
- `distDir` (string): директория dist

**Возвращает:**
```javascript
{
  total: 5,
  copied: 1,
  optimized: 4,
  failed: 0,
  errors: []
}
```

### processImagesInDirectory(sourceDir, distDir)

Обрабатывает изображения во всех markdown файлах директории.

**Параметры:**
- `sourceDir` (string): исходная директория
- `distDir` (string): директория dist

**Возвращает:**
```javascript
{
  filesProcessed: 15,
  totalImages: 42,
  copied: 4,
  optimized: 38,
  failed: 0,
  errors: []
}
```
