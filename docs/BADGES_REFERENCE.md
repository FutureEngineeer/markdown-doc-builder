# Badges Reference

## Быстрая справка по пометкам в визуализации

### Файлы

| Badge | Цвет | Значение | Действие |
|-------|------|----------|----------|
| `[hierarchy]` | 🟢 Зеленый | Файл явно указан в doc-config.yaml | Конвертируется в указанном порядке |
| `[auto-scanned]` | 🔵 Голубой | Файл найден автоматически | Конвертируется в алфавитном порядке |
| `[IGNORED]` | ⚫ Серый | Файл в списке ignored | **НЕ конвертируется** |

### Папки

| Badge | Цвет | Значение | Поведение |
|-------|------|----------|-----------|
| `[doc-config]` | 🟡 Желтый | Папка имеет doc-config.yaml | Структура контролируется конфигом |
| `[auto-scanned]` | 🔵 Голубой | Папка без конфига | Все файлы включаются автоматически |

### Специальные

| Badge | Цвет | Значение | Описание |
|-------|------|----------|----------|
| `[REPOSITORY]` | 🟣 Пурпурный | GitHub репозиторий | Скачивается и конвертируется |
| `[SECTION]` | 🔵 Синий | Секция (группа) | Контейнер для элементов |

## Иконки

| Иконка | Тип | Описание |
|--------|-----|----------|
| 📋 | README | readme.md, index.md, home.md |
| 📄 | File | Обычный markdown файл |
| 🚫 | Ignored | Игнорируемый файл |
| 📁 | Folder | Папка с doc-config.yaml |
| 📂 | Folder | Папка без конфига |
| 📦 | Repository | GitHub репозиторий |
| 📑 | Section | Секция (группа элементов) |

## Примеры

### Файл из hierarchy
```
📄 Getting Started [hierarchy]
```
- ✅ Конвертируется
- ✅ В указанном порядке
- ✅ С указанным title

### Автоматически найденный файл
```
📄 tutorial-1 [auto-scanned]
```
- ✅ Конвертируется
- ⚠️ В алфавитном порядке
- ⚠️ Title генерируется автоматически

### Игнорируемый файл
```
🚫 draft-article [IGNORED]
```
- ❌ НЕ конвертируется
- ❌ НЕ появляется в навигации
- ℹ️ Виден только в визуализации

### Папка с конфигом
```
📁 Guides [doc-config]
```
- ✅ Структура контролируется
- ✅ Порядок из hierarchy
- ✅ Можно игнорировать файлы

### Папка без конфига
```
📂 Examples [auto-scanned]
```
- ✅ Все файлы включены
- ⚠️ Алфавитный порядок
- ⚠️ Нельзя игнорировать файлы

### Репозиторий
```
📦 CLN Driver (cln) [REPOSITORY]
```
- ✅ Скачивается с GitHub
- ✅ Конвертируется полностью
- ℹ️ Alias в скобках

### Секция
```
📑 Projects [SECTION]
```
- ℹ️ Группирует элементы
- ℹ️ Не является папкой
- ℹ️ Определена в doc-config

## Как изменить badge

### Сделать файл [hierarchy]
Добавьте в doc-config.yaml:
```yaml
hierarchy:
  - file: myfile.md
    title: "My File"
```

### Сделать файл [IGNORED]
Добавьте в doc-config.yaml:
```yaml
ignored:
  - myfile.md
```

### Сделать папку [doc-config]
Создайте в папке файл `doc-config.yaml`:
```yaml
hierarchy:
  - file: readme.md
```

## Отладка по badges

### Файл не конвертируется?
1. Проверьте badge:
   - `[IGNORED]` → уберите из ignored
   - `[auto-scanned]` → должен конвертироваться
   - `[hierarchy]` → должен конвертироваться

### Неправильный порядок?
1. Проверьте badge:
   - `[auto-scanned]` → создайте doc-config с hierarchy
   - `[hierarchy]` → измените порядок в hierarchy

### Файл не в навигации?
1. Проверьте badge:
   - `[IGNORED]` → уберите из ignored
   - Нет в визуализации → проверьте путь к файлу

## Цветовая схема

```
🟢 [hierarchy]      - Зеленый  - Контролируется
🔵 [auto-scanned]   - Голубой  - Автоматически
🟡 [doc-config]     - Желтый   - Папка с конфигом
🟣 [REPOSITORY]     - Пурпурный - GitHub
🔵 [SECTION]        - Синий    - Секция
⚫ [IGNORED]        - Серый    - Игнорируется
```
