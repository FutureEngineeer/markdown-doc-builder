# Changelog - Build System v3

## Версия 3.0 - Параметризация корневой папки

**Дата:** 1 декабря 2024

### 🎯 Основные изменения

Система сборки теперь поддерживает указание корневой папки сайта через аргумент командной строки.

### ✨ Новые возможности

1. **Параметризованная сборка**
   - Команда принимает путь к корневой папке: `node build-all-v3.js <путь>`
   - Поддержка множественных сайтов в одном проекте
   - Автоматическое определение конфигурации из указанной папки

2. **Гибкая структура проекта**
   - Каждый сайт в отдельной папке с собственной конфигурацией
   - Независимые настройки для каждого сайта
   - Возможность создания неограниченного количества сайтов

3. **Улучшенная валидация**
   - Проверка существования корневой папки
   - Проверка наличия config.yaml
   - Информативные сообщения об ошибках

4. **Расширенная документация**
   - BUILD_USAGE.md - подробное руководство
   - EXAMPLE_SITE_STRUCTURE.md - примеры структур
   - БЫСТРЫЙ_СТАРТ.md - краткое руководство на русском
   - COMMANDS.md - справочник команд

### 🔧 Технические изменения

#### build-all-v3.js

**Было:**
```javascript
async function build() {
  const index = await indexAllFiles('website');
  // ...
}
```

**Стало:**
```javascript
async function build(rootPath = 'website') {
  // Валидация пути
  if (!fs.existsSync(rootPath)) {
    throw new Error(`Root path does not exist: ${rootPath}`);
  }
  
  // Проверка config.yaml
  const configPath = path.join(rootPath, 'config.yaml');
  if (!fs.existsSync(configPath)) {
    console.warn(`⚠️  Warning: config.yaml not found in ${rootPath}`);
  }
  
  const index = await indexAllFiles(rootPath);
  // ...
}

// Обработка аргументов командной строки
if (require.main === module) {
  const args = process.argv.slice(2);
  const rootPath = args[0] || 'website';
  build(rootPath);
}
```

#### package.json

**Обновлены скрипты:**
```json
{
  "scripts": {
    "build": "node build-all-v3.js website",
    "build:all": "node build-all-v3.js website",
    "build:netlify": "node build-all-v3.js website"
  }
}
```

### 📁 Структура проекта

**До:**
```
project/
├── build-all-v3.js
├── website/
│   └── (все файлы здесь)
└── dist/
```

**После:**
```
project/
├── build-all-v3.js
├── website/              # Сайт #1
│   ├── config.yaml
│   ├── doc-config.yaml
│   └── ...
├── docs-site/           # Сайт #2
│   ├── config.yaml
│   ├── doc-config.yaml
│   └── ...
├── blog/                # Сайт #3
│   ├── config.yaml
│   ├── doc-config.yaml
│   └── ...
└── dist/                # Результат сборки
```

### 📖 Использование

#### Базовая команда

```bash
node build-all-v3.js <root-path>
```

#### Примеры

```bash
# Сборка основного сайта
node build-all-v3.js website

# Сборка документации
node build-all-v3.js docs-site

# Сборка блога
node build-all-v3.js blog

# Использование по умолчанию (website)
node build-all-v3.js
```

#### NPM скрипты

```bash
# Стандартная сборка
npm run build

# Сборка с очисткой
npm run build:clean

# Сборка с оптимизацией
npm run build:optimized
```

### 🎨 Новые файлы документации

1. **BUILD_USAGE.md**
   - Подробное руководство по использованию
   - Описание процесса сборки
   - Примеры конфигурации
   - Советы по отладке

2. **EXAMPLE_SITE_STRUCTURE.md**
   - Минимальная структура сайта
   - Расширенные примеры
   - Структура с GitHub репозиториями
   - Быстрый старт

3. **БЫСТРЫЙ_СТАРТ.md**
   - Краткое руководство на русском
   - Основные команды
   - Примеры использования
   - Отладка

4. **COMMANDS.md**
   - Справочник всех команд
   - Параметры командной строки
   - Примеры автоматизации
   - CI/CD интеграция

### 🔄 Обратная совместимость

Система полностью обратно совместима:
- Если не указан путь, используется `website` по умолчанию
- Существующие проекты работают без изменений
- Все старые команды npm работают как прежде

### 🚀 Миграция

Для использования новой функциональности:

1. **Создайте новую папку для сайта:**
   ```bash
   mkdir my-new-site
   ```

2. **Добавьте конфигурацию:**
   - config.yaml
   - doc-config.yaml
   - home.md

3. **Запустите сборку:**
   ```bash
   node build-all-v3.js my-new-site
   ```

### 📊 Преимущества

1. **Гибкость**
   - Множественные сайты в одном проекте
   - Независимые конфигурации
   - Легкое переключение между сайтами

2. **Масштабируемость**
   - Неограниченное количество сайтов
   - Изолированные настройки
   - Простое добавление новых сайтов

3. **Удобство**
   - Простая командная строка
   - Понятная структура
   - Хорошая документация

4. **Надежность**
   - Валидация входных данных
   - Информативные ошибки
   - Обратная совместимость

### 🐛 Исправленные проблемы

- Жестко закодированный путь `website` в build-all-v3.js
- Невозможность создания нескольких независимых сайтов
- Отсутствие валидации корневой папки

### 📝 Примечания

- Все существующие функции сохранены
- Производительность не изменилась
- Формат конфигурации остался прежним
- Структура выходных файлов не изменилась

### 🔮 Будущие улучшения

Возможные направления развития:
- Поддержка конфигурационного файла для множественных сайтов
- Параллельная сборка нескольких сайтов
- Режим наблюдения (watch mode) для автоматической пересборки
- Интерактивный режим выбора сайта
- Шаблоны для быстрого создания новых сайтов

### 📞 Поддержка

При возникновении проблем:
1. Проверьте документацию в BUILD_USAGE.md
2. Изучите примеры в EXAMPLE_SITE_STRUCTURE.md
3. Прочитайте БЫСТРЫЙ_СТАРТ.md
4. Проверьте логи сборки
5. Просмотрите .temp/build-report.json

---

**Автор:** Build System Team  
**Версия:** 3.0.0  
**Дата:** 1 декабря 2024
