# doc-config.yaml - Шпаргалка

## Быстрый старт

```yaml
hierarchy:
  - Название: путь
```

## Типы элементов

### 📄 Файл
```yaml
- Home: home.md
```
или
```yaml
- Home:
    path: home.md
    description: "Описание"
```

### 📁 Папка
```yaml
- Docs: docs/
```
или
```yaml
- Docs:
    path: docs/
    description: "Документация"
```

### 📦 GitHub репозиторий
```yaml
- Repo: "https://github.com/user/repo"
```
или
```yaml
- Repo:
    path: "https://github.com/user/repo"
    section: true
    description: "Описание репозитория"
```

### 📑 Секция с подразделами
```yaml
- Projects:
    section: true
    sub:
      - Alpha: project-alpha/
      - Beta: project-beta/
```

## Полный пример

```yaml
hierarchy:
  - Home:
      path: home.md
      description: "Главная страница"
  
  - Docs:
      path: docs.md
  
  - GitHub Repo:
      path: "https://github.com/user/repo"
      section: true
  
  - Projects:
      section: true
      sub:
        - Alpha: project-alpha/
        - Beta: project-beta/
```

## Параметры

| Параметр | Тип | Описание |
|----------|-----|----------|
| `path` | string | Путь к файлу/папке/URL (обязательный) |
| `description` | string | Описание элемента |
| `alias` | string | Пользовательский алиас для URL |
| `section` | boolean | Отображать как секцию |
| `sub` | array | Дочерние элементы (для секций) |

## Правила путей

- **Файл**: `file.md`
- **Папка**: `folder/` (с `/` в конце)
- **Репозиторий**: `https://github.com/user/repo`

## Советы

✅ Используйте краткий формат для простых элементов
✅ Используйте полный формат когда нужны description или alias
✅ Порядок в hierarchy = порядок в меню
✅ Каждая папка может иметь свой doc-config.yaml

❌ Файлы вне hierarchy не будут конвертироваться
❌ Не забывайте `/` в конце пути к папке
