# Быстрый старт автоматического деплоя

## 🚀 Настройка за 5 минут

### 1. Подготовка репозитория

```bash
# Установите зависимости
npm install

# Обновите конфигурацию
# Отредактируйте config.yaml - укажите ваши репозитории и настройки
```

### 2. GitHub Pages (рекомендуется)

1. **Включите GitHub Pages:**
   - Перейдите в Settings → Pages
   - Source: "GitHub Actions"
   - Сохраните

2. **Проверьте конфигурацию:**
   ```yaml
   # config.yaml
   site:
     baseUrl: "https://ваш-username.github.io/ваш-репозиторий"
   
   build:
     input:
       githubRepositories:
         - url: "https://github.com/username/repo1"
           description: "Описание репозитория"
           alias: "Repo1"  # Псевдоним (опционально)
   ```

3. **Запустите первую сборку:**
   ```bash
   git add .
   git commit -m "Setup automatic deployment"
   git push origin main
   ```

### 3. Netlify (альтернатива)

1. **Подключите репозиторий:**
   - Войдите в Netlify
   - "New site from Git" → выберите репозиторий
   - Настройки подтянутся автоматически из `netlify.toml`

2. **Готово!** Netlify автоматически соберет и задеплоит сайт

## 🔧 Локальное тестирование

```bash
# Тестовая сборка
npm run build:test

# Обычная сборка
npm run build:all

# Принудительная пересборка
npm run build:force

# Очистка кеша
npm run cache:clear

# Информация о кеше
npm run cache:info
```

## 📋 Что происходит автоматически

### ✅ GitHub Pages
- ⏰ Проверка изменений каждые 12 часов
- 🔄 Обновление только измененных репозиториев  
- 💾 Кеширование для ускорения сборки
- 🚀 Автоматический деплой при изменениях

### ✅ Netlify
- 🔄 Автоматическая пересборка при пуше
- 💾 Встроенное кеширование
- 🚀 Мгновенный деплой
- 📊 Оптимизация ресурсов

## 🎯 Основные команды

| Команда | Описание |
|---------|----------|
| `npm run build:all` | Обычная сборка с кешем |
| `npm run build:force` | Принудительная пересборка |
| `npm run build:test` | Тестовая сборка с отчетом |
| `npm run cache:clear` | Очистить весь кеш |
| `npm run cache:info` | Показать информацию о кеше |

## 🔍 Мониторинг

### GitHub Actions
- Перейдите в Actions → Build and Deploy
- Смотрите логи сборки и статус

### Netlify
- Dashboard → Site overview
- Deploys → Deploy log

## ⚡ Оптимизация

### Ускорение сборки
1. **Не отключайте кеш** без необходимости
2. **Добавляйте только нужные репозитории**
3. **Используйте `.env` для настроек**

### Экономия ресурсов
1. **12-часовой интервал** предотвращает частые сборки
2. **Инкрементальные обновления** экономят время
3. **Умное кеширование** ускоряет повторные сборки

## 🆘 Решение проблем

### Сборка не запускается
```bash
# Проверьте права доступа
# Settings → Actions → General → Workflow permissions

# Проверьте файлы workflow
ls -la .github/workflows/
```

### Кеш не работает
```bash
# Очистите и пересоздайте
npm run cache:clear
npm run build:all
```

### Репозитории не обновляются
```bash
# Проверьте конфигурацию
node -e "console.log(require('js-yaml').load(require('fs').readFileSync('config.yaml', 'utf8')))"
```

## 📚 Дополнительно

- 📖 [Полная документация](DEPLOYMENT_README.md)
- 🔧 [Настройка аналитики](GOOGLE_ANALYTICS_INTEGRATION.md)
- 📝 [Конфигурация](CONFIG_README.md)

---

**🎉 Готово!** Ваш сайт будет автоматически обновляться при изменениях в отслеживаемых репозиториях.