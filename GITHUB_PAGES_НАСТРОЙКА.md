# Настройка GitHub Pages

## 🚀 Быстрый старт

### Шаг 1: Включите GitHub Pages

1. Откройте настройки репозитория: **Settings** → **Pages**
2. В разделе **Source** выберите **GitHub Actions**
3. Сохраните

### Шаг 2: Обновите baseUrl

Откройте `config.yaml` и измените:

```yaml
site:
  baseUrl: "https://ваш-username.github.io/название-репозитория"
```

Например:
```yaml
site:
  baseUrl: "https://futureengineeer.github.io/markdown-doc-builder"
```

### Шаг 3: Запустите деплой

Сделайте коммит и пуш в ветку `main`:

```bash
git add .
git commit -m "Configure GitHub Pages"
git push
```

Деплой запустится автоматически! 🎉

## ✅ Проверка

1. Перейдите в **Actions** - должен запуститься workflow
2. Дождитесь завершения (зеленая галочка)
3. Откройте ваш сайт: `https://ваш-username.github.io/название-репозитория`

## 🔧 Доступные команды

```bash
# Локальная сборка
npm run build:all

# Сборка с оптимизацией
npm run build:optimized

# Умная оптимизация изображений (только новые)
npm run optimize:images:smart

# Анализ изображений
npm run optimize:images

# Очистка кеша
npm run cache:clear

# Информация о кеше
npm run cache:info
```

## 📊 Workflows

### 1. Deploy to GitHub Pages (основной)
- Запускается при пуше в `main`
- Оптимизирует только новые изображения
- Использует кеш для быстрой сборки

### 2. Build and Deploy (Advanced) (продвинутый)
- Запускается по расписанию (каждые 12 часов)
- Автоматически обновляет внешние репозитории
- Можно запустить вручную с полной пересборкой

## 🎯 Оптимизация

### Умная оптимизация изображений

Система автоматически:
- ✅ Отслеживает уже оптимизированные файлы
- ✅ Оптимизирует только новые или измененные изображения
- ✅ Сохраняет до 80% размера для GIF
- ✅ Сохраняет до 30% размера для PNG/JPEG

### Кеширование

Кешируются:
- Скачанные репозитории
- Оптимизированные изображения
- Метаданные

Это ускоряет сборку с 5 минут до 30-60 секунд!

## 🐛 Устранение проблем

### Ошибка 404

Проверьте `baseUrl` в `config.yaml` - он должен соответствовать вашему репозиторию.

### Изображения не оптимизируются

Установите пакеты:

```bash
npm install --save-dev imagemin imagemin-mozjpeg imagemin-pngquant imagemin-gifsicle imagemin-svgo
```

### Репозитории не обновляются

Запустите вручную **Build and Deploy (Advanced)** с опцией `force_rebuild: true`

## 📖 Подробная документация

См. [GITHUB_PAGES_SETUP.md](GITHUB_PAGES_SETUP.md) для полной документации на английском.

## 💡 Полезные советы

1. **Первая сборка** занимает 2-5 минут (скачивание репозиториев)
2. **Последующие сборки** занимают 30-60 секунд (с кешем)
3. **Автообновление** внешних репозиториев каждые 12 часов
4. **Оптимизация** изображений происходит автоматически

## 🎉 Готово!

Ваш сайт теперь доступен по адресу:
`https://ваш-username.github.io/название-репозитория`

Все изменения будут автоматически деплоиться при пуше в `main`!
