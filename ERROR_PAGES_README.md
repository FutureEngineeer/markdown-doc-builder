# Генератор страниц ошибок

## Описание

Автоматический генератор красивых анимированных страниц ошибок для вашего сайта с эффектом глитча и космической анимацией.

## Возможности

✨ **10 типов страниц ошибок**: 400, 401, 402, 403, 404, 500, 501, 502, 503, 504

🎨 **Стильный дизайн**:
- Анимация глитча на коде ошибки
- Космическая анимация частиц на фоне
- Логотип и название сайта из конфигурации
- Адаптивный дизайн для мобильных устройств

💬 **20 случайных слоганов** для каждого типа ошибки на английском языке
- При каждом обновлении страницы выводится новый слоган
- Прикольные технические шутки и мемы для разработчиков

🔧 **Встроенные стили и скрипты**:
- Все CSS и JS встроены напрямую в HTML
- Нет зависимости от внешних файлов
- Работает даже при проблемах с загрузкой assets
- Один HTTP запрос вместо трех

## Использование

### Генерация страниц

Страницы ошибок генерируются автоматически при каждой сборке проекта:

```bash
# Основная сборка (включает генерацию страниц ошибок)
npm run build

# Или отдельно только страницы ошибок
npm run errors:generate
```

Страницы будут созданы в корне папки `dist/`:
- `400.html` - Bad Request
- `401.html` - Unauthorized
- `402.html` - Payment Required
- `403.html` - Forbidden
- `404.html` - Page Not Found
- `500.html` - Internal Server Error
- `501.html` - Not Implemented
- `502.html` - Bad Gateway
- `503.html` - Service Unavailable
- `504.html` - Gateway Timeout

### Настройка веб-сервера

#### Nginx

```nginx
server {
    # ... другие настройки ...
    
    error_page 400 /400.html;
    error_page 401 /401.html;
    error_page 403 /403.html;
    error_page 404 /404.html;
    error_page 500 /500.html;
    error_page 502 /502.html;
    error_page 503 /503.html;
    error_page 504 /504.html;
}
```

#### Apache (.htaccess)

```apache
ErrorDocument 400 /400.html
ErrorDocument 401 /401.html
ErrorDocument 403 /403.html
ErrorDocument 404 /404.html
ErrorDocument 500 /500.html
ErrorDocument 502 /502.html
ErrorDocument 503 /503.html
ErrorDocument 504 /504.html
```

#### GitHub Pages

Для GitHub Pages скопируйте файл `404.html` в корень репозитория:

```bash
cp dist/404.html 404.html
```

#### Netlify

Создайте файл `netlify.toml` (или обновите существующий):

```toml
[[redirects]]
  from = "/*"
  to = "/404.html"
  status = 404

[[redirects]]
  from = "/*"
  to = "/500.html"
  status = 500
```

## Настройка

Генератор автоматически использует настройки из `config.yaml`:

- `site.name` - название сайта (например: "creapunk")
- `site.title` - подзаголовок сайта (например: "motion control systems")
- `icons.site.logo` - путь к логотипу (например: "./assets/creapunk-icon.svg")
- `site.logoClickUrl` - ссылка на главную страницу (например: "index.html")

## Кастомизация

### Изменение стилей

1. Отредактируйте файл `assets/styles/error-page.css`:
   - Цвета глитч-эффекта
   - Размеры и шрифты
   - Анимации
   - Адаптивность

2. Перегенерируйте страницы:
   ```bash
   npm run errors:generate
   ```

Стили автоматически встроятся в HTML.

### Изменение анимации

1. Отредактируйте файл `assets/scripts/error-page.js`:
   - Количество частиц
   - Скорость анимации
   - Размер зоны исключения
   - Эффекты затухания

2. Перегенерируйте страницы:
   ```bash
   npm run errors:generate
   ```

Скрипты автоматически встроятся в HTML.

### Изменение текстов

1. Отредактируйте массив `errorMessages` в файле `components/errorPageGenerator.js`
2. Перегенерируйте страницы:
   ```bash
   npm run errors:generate
   ```

## Примеры слоганов

### 400 - Bad Request
- "Syntax error in reality."
- "You divided by zero, didn't you?"
- "JSON parse error: Life."
- "RTFM and try again."

### 401 - Unauthorized
- "Sudo make me a sandwich."
- "401: Thou shall not pass."
- "Wrong password, try 'admin'."
- "Ghost in the shell only."

### 402 - Payment Required
- "Insert coin to continue."
- "Your wallet is 404."
- "Send Bitcoin to proceed."
- "Need gold to respawn."

### 403 - Forbidden
- "Verboten!"
- "Don't touch the prod DB."
- "Chmod 777 won't help."
- "Access level: Peasant."

### 404 - Page Not Found
- "The cake is a lie."
- "File moved to /dev/null."
- "404: Brain not found."
- "Abyss staring back."

### 500 - Internal Server Error
- "Server on fire."
- "It works on my machine!"
- "Magic smoke escaped."
- "Deploying hotfix..."

### 501 - Not Implemented
- "TODO: Fix this later."
- "Feature coming soon™."
- "Ask again in 2030."
- "Project abandoned?"

### 502 - Bad Gateway
- "Nginx is confused."
- "Network gremlins."
- "Echo... echo... echo..."
- "Communication breakdown."

### 503 - Service Unavailable
- "Server needs a nap."
- "DDoS or just popular?"
- "Hug of death."
- "Temporarily bricked."

### 504 - Gateway Timeout
- "Waiting for Godot."
- "Ping: 9999ms."
- "Did it crash? Probably."
- "Infinite wait loop."

## Технические детали

- **Анимация**: Canvas API для космического эффекта
- **Эффект глитча**: CSS анимации с clip-path
- **Оптимизация**: Автоматическая пауза анимации при неактивной вкладке
- **Производительность**: 200 частиц с оптимизированным рендерингом
- **Размер**: ~12.5 KB на страницу (включая все стили и скрипты)
- **Встраивание**: CSS и JS автоматически встраиваются в HTML при генерации

## Структура файлов

```
├── assets/
│   ├── styles/
│   │   └── error-page.css      # Стили страниц ошибок
│   └── scripts/
│       └── error-page.js        # Анимация и логика
├── scripts/
│   └── generate-error-pages.js  # Генератор страниц
└── dist/
    ├── 400.html                 # Страницы ошибок
    ├── 401.html
    ├── 403.html
    ├── 404.html
    ├── 500.html
    ├── 502.html
    ├── 503.html
    └── 504.html
```

## Обновление

Чтобы обновить страницы после изменения конфигурации или стилей:

```bash
npm run errors:generate
```

Страницы будут перегенерированы с новыми настройками.
