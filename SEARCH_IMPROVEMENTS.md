# 🔍 Улучшения поиска

## Реализованные улучшения

### 1. Горизонтальные разделители между страницами
Добавлены визуальные разделители между результатами с разных страниц для лучшей читаемости.

### 2. Группировка совпадений на одной странице
Если на странице найдено несколько совпадений, они группируются под заголовком `N more on this page` (без #).

**Особенности:**
- Для текущей страницы группа раскрыта по умолчанию
- Для остальных страниц группа свернута (нужен клик для раскрытия)
- Анимированная стрелка показывает состояние (▼ раскрыто, ▶ свернуто)

### 3. Контекстный вывод совпадений со стилями
- **Умное ограничение контекста**: максимум 50 слов или до конца абзаца
- Сохраняются все стили сайта (код, списки, таблицы, форматирование)
- Если есть ближайший заголовок, он отображается над контекстом
- Каждое совпадение подсвечивается с помощью `<mark>`
- Поддержка: `<code>`, `<pre>`, `<ul>`, `<ol>`, `<strong>`, `<em>`, `<table>`
- Все заголовки (h1-h6) отображаются одинаково: небольшие и жирные

### 4. Фильтрация служебных элементов
Из индекса исключены:
- Header (шапка сайта)
- Footer (подвал)
- Hamburger menu (боковое меню)
- Section map (карта разделов)
- Navigation (навигация)

Индексируется только контент из `<main>` элемента.

### 5. Привязки к заголовкам (якорные ссылки)
**ВСЕГДА** создается привязка к ближайшему заголовку:
- При клике на результат открывается страница с прокруткой к нужному разделу
- Используются ID заголовков из HTML (`#heading-id`)
- Для каждого совпадения определяется ближайший заголовок выше по тексту

### 6. Приоритет текущей страницы
Результаты с текущей страницы отображаются первыми с визуальным индикатором "Current Page".

### 7. Индексация заголовков и таблиц
**Заголовки** индексируются как отдельное поле с высоким приоритетом (boost: 8):
- Поиск по заголовкам дает более релевантные результаты
- Все уровни заголовков (h1-h6) включены в индекс
- Заголовки имеют приоритет выше, чем обычный текст, но ниже, чем title страницы

**Таблицы** полностью индексируются:
- Текст из ячеек таблиц включен в поисковый индекс
- HTML структура таблиц сохраняется в результатах
- Таблицы отображаются со стилями сайта в превью результатов

### 8. Исключение страниц ошибок
Из индекса автоматически исключаются:
- Страницы с именами: `404.html`, `500.html`, `403.html`, `401.html`, `503.html`
- Страницы с заголовками, содержащими: "404", "500", "error", "not found"
- Это предотвращает появление служебных страниц в результатах поиска

## Технические детали

### Изменения в `components/searchIndex.js`

1. **Функция `extractMainContent()`** - извлекает только контент из `<main>`
2. **Функция `extractHeadings()`** - извлекает заголовки с ID и позициями для якорных ссылок
3. **Функция `extractMainHtml()`** - извлекает HTML с сохранением стилей
4. **Улучшенная `extractTextFromHtml()`** - фильтрует служебные элементы
5. **Фильтрация страниц ошибок** - проверка по имени файла и заголовку
6. **Индексация заголовков** - создание отдельного поля `headings` с boost: 8

```javascript
// Пример структуры документа в индексе
{
  id: "0",
  title: "Page Title",
  headings: "Heading 1 Heading 2 Heading 3", // Для поиска
  headingsData: [                             // Для навигации
    { level: "h1", id: "heading-1", text: "Heading 1", position: 100 },
    { level: "h2", id: "heading-2", text: "Heading 2", position: 500 }
  ],
  content: "Plain text content...",
  contentHtml: "<p>HTML content...</p>",
  url: "page.html",
  breadcrumb: "Section / Page",
  section: "Section"
}
```

### Изменения в `assets/scripts/search.js`

1. **Функция `findMatches()`** - находит все совпадения в документе
2. **Функция `getCurrentPageUrl()`** - определяет текущую страницу
3. **Улучшенная `performSearch()`**:
   - Сортировка с приоритетом текущей страницы
   - Группировка совпадений по документам
   - До 30 результатов (вместо 20)
   - До 5 совпадений на документ

4. **Улучшенная `handleSearchInput()`**:
   - Группировка результатов по страницам
   - Отображение дополнительных совпадений
   - Разделители между страницами
   - Бейдж "Current Page"

### Изменения в `assets/styles/search.css`

Новые стили:
- `.search-result-separator` - горизонтальный разделитель
- `.search-more-matches` - контейнер для дополнительных совпадений
- `.search-more-matches-header` - заголовок "# N more on this page"
- `.search-match-item` - элемент дополнительного совпадения
- `.current-page-badge` - бейдж текущей страницы
- `.search-result-item.current-page` - стиль для результата с текущей страницы

### Изменения в `assets/styles.css`

Добавлена переменная `--accent-rgb: 47, 182, 90` для использования прозрачности accent цвета.

## Пример использования

1. Откройте любую страницу в `dist/`
2. Нажмите на кнопку поиска (лупа) или `Ctrl+K`
3. Введите поисковый запрос
4. Результаты отобразятся с:
   - Приоритетом для текущей страницы (если есть совпадения)
   - Группировкой совпадений на одной странице
   - Разделителями между разными страницами
   - Контекстом вокруг каждого совпадения

## Технические детали реализации

### Умное ограничение контекста

```javascript
function extractSmartContext(content, position, maxWords = 50) {
  // Находим границы абзаца
  let paragraphStart = content.lastIndexOf('\n\n', position);
  let paragraphEnd = content.indexOf('\n\n', position);
  
  let context = content.substring(paragraphStart, paragraphEnd).trim();
  
  // Если абзац > 50 слов, обрезаем вокруг совпадения
  if (countWords(context) > maxWords) {
    // Берем 25 слов до и 25 после совпадения
    context = extractWordsAround(context, position, maxWords);
  }
  
  return context;
}
```

### Унификация заголовков в результатах

```css
.search-result-preview h1,
.search-result-preview h2,
.search-result-preview h3,
.search-result-preview h4,
.search-result-preview h5,
.search-result-preview h6 {
    font-size: 0.95rem !important;
    font-weight: 600 !important;
}
```

### Сворачиваемые группы

```javascript
function toggleMoreMatches(id) {
  const element = document.getElementById(id);
  if (element) {
    element.classList.toggle('collapsed');
  }
}
```

CSS анимация:
```css
.search-more-matches.collapsed .search-more-matches-content {
    max-height: 0;
    opacity: 0;
}
```

### Привязка к заголовкам

```javascript
function findNearestHeading(headings, position) {
  let nearest = null;
  for (const heading of headings) {
    if (heading.position <= position) {
      nearest = heading;
    } else {
      break;
    }
  }
  return nearest;
}
```

Каждый результат получает якорь:
```javascript
const anchor = match.heading ? `#${match.heading.id}` : '';
href="${getResultUrl(result.url)}${anchor}"
```

### Сохранение HTML стилей

В индексе сохраняется два вида контента:
- `content` - чистый текст для поиска
- `contentHtml` - HTML разметка для отображения со стилями

## Будущие улучшения

- [ ] Подсветка найденного текста на странице после перехода
- [ ] Фильтры по типу контента (заголовки, текст, код)
- [ ] История поисковых запросов
- [ ] Автодополнение запросов
- [ ] Поиск по регулярным выражениям
